"""Voyage AI embedding generation for semantic matching."""

from __future__ import annotations


def embed_patient(record: dict) -> list[float]:
    """Generate a Voyage AI embedding for a patient record."""
    raise NotImplementedError("Week 2 — implement after Voyage API key is configured")
