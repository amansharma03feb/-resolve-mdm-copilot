"""LangGraph agent graph — orchestrate match triage workflow."""

from __future__ import annotations


def build_triage_graph():
    """Build the LangGraph state machine for match triage."""
    raise NotImplementedError("Week 3+ — implement after individual nodes work")
